[run]
# Include your Django apps (adjust paths as needed)
source =
    zssn_app

# Exclude migrations and tests
omit =
    */migrations/*
    */tests/*

[report]
# Fail if coverage is below 80%
fail_under = 80

# Output formats (terminal + HTML report)
show_missing = True